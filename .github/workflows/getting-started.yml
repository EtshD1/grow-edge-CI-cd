name: Check Health

on:
  push:
    branches:
      - main
      - dev
  workflow_dispatch:

jobs:
  first-job:
    runs-on: ubuntu-latest

    steps:
      - name: Say hello
        run: echo "Hello!"
